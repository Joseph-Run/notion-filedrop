[server]
# Browser-side ceiling. The app's own MAX_UPLOAD_MB is the limit visitors actually
# see; this just has to be at least that big (video needs headroom).
maxUploadSize = 200

[browser]
gatherUsageStats = false

[theme]
base = "light"
